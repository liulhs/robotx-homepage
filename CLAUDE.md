# robotx-homepage

Reference mirror of https://www.fieldai.com/ under `fieldai-mirror/`, used as design reference for a future RobotX homepage build.

## Git / GitHub

- Always commit and push using the user's GitHub credentials (`liulhs`). Never attribute commits to Claude.
- No `Co-Authored-By: Claude ...` trailers on any commit, ever.
- `gh` CLI is already authenticated as `liulhs` — use it for any repo operations.
